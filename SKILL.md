---
name: watermark-remover
description: 清除 PDF 文档里的水印与推广信息（平铺文字水印、页脚公众号/二维码推广、图形印章）。当用户要求「去水印」「清除水印」「清掉这份 PDF 的广告」「去掉页脚的公众号」，或需要批量清理客户发来的资料 PDF 再交付时使用。只做 PDF；图片/视频/Office 暂未实现。绝不使用红删涂白（会连带擦掉格线表格线）。
---

# 通用水印清理

面向「客户发来的 PDF 带着平台水印，要清干净再交付」这类需求。

## 核心原则：先报告，后执行

**永远不要一上来就删。** 水印和正文常常长得一样（详见下面的陷阱），
必须先把候选列出来、判断清楚，再动手。

```bash
WM=/home/zcl/.pi/agent/skills/watermark-remover/wm
"$WM" analyze 文档.pdf                    # ① 只读扫描，输出候选报告（绝不动文件）
"$WM" remove  文档.pdf --ids 8,15 --yes   # ② 按你的判断执行
```

`wm remove` 默认只预演（打印「将要删除什么」但不写文件），加 `--yes` 才真正执行。
输出写到 `<名字>-cleaned.pdf`，**原文件永不改动**。

## 判读报告

报告把每个候选分三档：

| 档位 | 含义 | 怎么处理 |
|---|---|---|
| `保留` | 几何判定是**线条结构**（高覆盖率贯穿线），文字层可能被伪造 | **不要删**，这是图案/辅助线 |
| `删★` | 文本含推广关键词（公众号/水印/会员/扫码…） | 通常可直接删 |
| `待定` | 确认是文字，但线索不足 | **你来判断**（见下） |

**待定项怎么判断** —— 报告给的线索（重复次数、是否在页边）都**不足以自动定罪**：

- **重复出现 ≠ 水印**：练字帖的范字、表格的表头、模板的固定文案都会重复
- **在页边 ≠ 水印**：标题、页眉、页码也在页边
- **真正该问的是**：这段文字是**文档内容**，还是**发布者的推广/署名**？

判断线索：内容是客户要用的东西（正文、标题、数据、范字）→ 留；
内容是「谁做的、怎么联系、去哪买」→ 删。

遇到拿不准的，**问用户**，不要猜。

## 关键陷阱（开发中真踩过的坑）

### 1. 文字层会撒谎（ToUnicode 造假）

生成方可以把**任意字形**的 ToUnicode 映射成**任意文本**。
真实遇到的实例：120 个「田字格辅助线」的字形，文字层读出来是「速创字帖」——
按文字匹配删除，就把格子线全删了，而文字层看起来完全正常。

**所以 `wm` 不轻信文字层**：对每个候选做**字形几何分析**
（统计该字形自身颜色的像素，看投影是否存在高覆盖率贯穿线）。
线条结构 → 判定 `保留`。

这条判据在该实例中 100% 分离：辅助线横贯 4~9 行/列，所有汉字均为 0/0。

### 2. 绝不能「红删/涂白」

那会把水印**底下的**内容（格线、表格线、背景图案）一起抹掉。
正确做法是**内容流块级手术**：只摘掉画水印的那条绘制指令
（文字块 BT…ET 或图片调用 Do），底下的东西分毫不动。

### 3. 保存时不能用 garbage>=2

PyMuPDF 的 garbage=4 会**重排对象编号**，让手写的资源引用失效
（开发中曾因此把内嵌装饰图弄丢）。用 garbage=1。

### 4. 删完必须量化验证

肉眼看图不一定发现问题。`wm remove --yes` 会自动跑验证：

- **保护类**：矢量对象数、内嵌图片数、页数 —— 必须不变
- **消失类**：目标水印文本 —— 必须归零
- **颜色类**：绿色像素（格线典型色）变化超过 5% 就报警

验证不通过会返回非零退出码。

## 环境

工具自带 venv，直接调用即可（`wm` 是 bash 包装，会自动用 `./.venv/bin/python`）：

```bash
WM=/home/zcl/.pi/agent/skills/watermark-remover/wm
"$WM" analyze 文档.pdf
```

若 venv 丢失：

```bash
cd /home/zcl/.pi/agent/skills/watermark-remover
bash install.sh
```

## 目录结构

```
watermark-remover/
├── wm                      CLI 入口（bash 包装 → .venv）
├── wm.py                   CLI 实现
├── wmkit/pdf/
│   ├── glyph.py            字形几何分析（识破 ToUnicode 造假）★核心
│   ├── analyze.py          候选识别 + 风险评级 + 报告渲染
│   ├── apply.py            内容流块级手术
│   └── verify.py           量化验证
├── wmkit/office/           （规划中）解压改 XML
├── wmkit/image/            （规划中）定位 + 修复
├── tests/                  回归测试（用真实案例）
└── samples/                测试样本
```

---

# 图片去水印（`wm image`）

图片与 PDF **原理完全不同**：PDF 的水印是独立对象（删掉即可，正文分毫不动），
图片的水印**融进了像素**：`observed = original×(1-a) + C×a`。
只能**反算**（知道强度 a 与颜色 C 时）或**重绘**（inpainting）来近似还原。

## 实测结论（决定了工具的设计）

| 手段 | 与水印真值的差距 |
|---|---|
| **反算**（a 正确） | **1.25 / 255** ← 近乎无损 |
| inpainting | 33.6 / 255 |

**反算比 inpainting 好 26 倍**，所以优先用它。

**但 a（水印强度）不能自动猜**：实测用均值法、方差法估计都会失准
（水印笔画细、背景有纹理时尤其糟）。所以设计成 **a 由人定**：
`wm image try` 一次生成 6 个不同 a 的对比图，挑一张最干净的。

## 用法

```bash
WM=/home/zcl/.pi/agent/skills/watermark-remover/wm

"$WM" image info 图.png                    # 先看尺寸与可用线索

# ① 生成 a 候选对比图（6 宫格，挑一个）
"$WM" image try 图.png --rect 72,40,150,104 --rect 332,40,410,104

# ② 按挑好的 a 执行
"$WM" image remove 图.png --rect 72,40,150,104 --rect 332,40,410,104 \
      --alpha 0.32 --yes

"$WM" image batch ./图片目录 --alpha 0.32 --bright 97 --yes
```

## 遮罩来源（三选一，按可靠性排序）

| 方式 | 适用 | 可靠性 |
|---|---|---|
| **`--rect x0,y0,x1,y1`** | 位置固定的水印，用户能框出来（可重复多次） | ★★★ 最可靠 |
| **`--template 样本.png`** | 有纯净的水印样本图 | ★★ 样本要干净，否则匹配不准 |
| **`--bright N` / `--dark N`** | 半透明水印，快速试 | ★ 粗糙，会连亮背景一起圈进去 |
| **`--auto`** | 想完全不手工指定位置 | ★ **实验性，仅作粗筛** |

**关于 `--auto` 的实测结论（别期望过高）**：在自带样例（渐变天空 + 半透明白字）
上只能找到约 1/3 的水印像素、三处水印仅命中一处。原因是半透明叠加的对比度本就
很低（实测差值 ~20/255），而背景估计的核又要跨过水印尺度，两者互相牵制 ——
核小了背景把水印算进去，核大了渐变背景自己也被抹平。试过 31px/61px 核与多组
阈值，未能稳定解决。**图片定位要做到真正通用，正确路径是接入分割模型**
（如 LaMA / IOPaint），而不是继续堆启发式规则 —— 这也是本文档刻意标注的边界。

**`--grow`**（默认 2）：遮罩膨胀像素数，用来盖住水印边缘的抗锯齿残留。

## 操作建议

1. **先用 `try` 找对 a** —— 生成的对比图里，挑「残留最少、又没出现白斑/过曝」的那张
2. **遮罩宁小勿大** —— 框大了会把正常像素也反算，反而引入失真
3. **不透明水印（实心 logo）**：反算不适用，它会自动检测越界像素并交给重绘兜底
4. **检查结果**：`remove` 会打印「遮罩内均值 vs 周边均值」，两者接近说明修得自然

---

# Office 文档去水印（`wm office`）

DOCX / PPTX / XLSX 本质是 **ZIP 包 + 一堆 XML**，水印通常在：
页眉页脚（`header*.xml` / `footer*.xml`）、正文（`document.xml` / `slide*.xml`）、
内嵌图片（`media/*` logo 与二维码）。

处理方式是**解压 → 删对应节点 → 按原结构重新打包**，只动该动的部件。
没有 PDF 那种 ToUnicode 陷阱，但**包结构不能乱**（条目顺序、`[Content_Types].xml`
位置都有讲究），所以工具会逐条原样写回，并在删完后自动核对结构是否一致。

```bash
WM=/home/zcl/.pi/agent/skills/watermark-remover/wm

"$WM" office analyze 文件.docx -v          # 看候选（-v 连待定的一起列）
"$WM" office remove  文件.docx --auto --yes   # 删关键词命中的
"$WM" office remove  文件.docx --ids 2,3 --yes
"$WM" office remove  文件.docx --drop-media word/media/image2.png --yes  # 删 logo/二维码
"$WM" office batch   ./文档目录 --yes
```

评级规则与 PDF 一致：**只有含推广关键词的自动判删**，其余交人工。
删完自动验证「目标文字已清除 + 包结构与原件一致」，不一致会返回非零退出码。

**注意**：默认**整段删除**（页眉页脚的推广文字通常独占一段）；
若只想清空文字保留段落，加 `--keep-paragraph`。

---

# 视频去水印（`wm video`）

视频比图片多一个维度上的约束：**时序一致性**。逐帧独立修复会让原本静止的
水印处"忽明忽暗"地闪 —— 实测逐帧 inpaint 的帧间抖动（0.42）比不处理时
水印本身（0.25）**还大**，也就是"修完比不修更难看"。所以本模块固定带一道
**时域稳定**工序。

```bash
WM=/home/zcl/.pi/agent/skills/watermark-remover/wm

"$WM" video info 片子.mp4                    # 看规格
"$WM" video probe 片子.mp4                   # 定位水印（实验性），出标注图
"$WM" video remove 片子.mp4 --rect 415,290,600,350 --yes
"$WM" video batch ./视频目录 --rect 415,290,600,350 --yes
```

## 定位：**框选最可靠**

水印位置在一段片子里通常固定，**框一次就够**。自动定位（`probe`）是实验性的：

- 半透明水印**不会让运动停止**（是透过水印看内容），所以"找完全静止区域"会
  检出 0；改用「时间波动被压缩」的比值判据后能检出，但**笔画太细时碎片化**
  （波动被压低 (1-a) 倍，量级接近噪声），只适合给粗线索
- 本机**无 NVIDIA GPU**，ProPainter / E2FGVI 这类模型方案不现实（效果最好但要显卡）

## 修复方式

| 方式 | 何时用 | 实测 |
|---|---|---|
| **inpaint（默认）** | 一般情况 | 误差 1.10 / 抖动 0.076 |
| **`--alpha` 反算** | 明确知道水印强度时 | 单帧更准，但 alpha 估偏时反而崩（实测 31.11 vs 1.48）|

**`--temporal`**（默认 1）：时域稳定的窗口半径，取相邻帧修复结果的中值。
默认开启，**不要关**（`--temporal 0` 只在做对比实验时用）。

## 实测数据（640×360 合成片，半透明白字）

```
不处理（基线）      水印区误差  7.27    帧间抖动 0.126
处理后              水印区误差  1.10    帧间抖动 0.076   ← 又准又稳
```

音轨会被原样保留（`-c:a copy`），帧率、时长不变。

---

## 已完成 / 未完成

**已完成**：
- **PDF 引擎** —— 识别（半透明度为主判据，内容无关）、字形几何判据（识破 ToUnicode 造假）、内容流手术、量化验证
- **图片引擎** —— 多种遮罩来源 + 反算/重绘 + **LaMA 模型修复**
- **Office 引擎** —— OOXML 结构感知，包结构原样保留
- **视频引擎** —— 逐帧修复 + **时域稳定**（实测抖动 0.148→0.071）

**模型**：LaMA ONNX（`models/lama_fp32.onnx`，208MB）。
国内直连 GitHub / HuggingFace 不通，需从 **hf-mirror.com** 取：

```bash
cd /home/zcl/.pi/agent/skills/watermark-remover
curl -L -o models/lama_fp32.onnx \
  https://hf-mirror.com/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx
```

运行时用 **onnxruntime**（约 15MB），不需要 torch。

**未实现**：图片/视频的**全自动定位** —— 经典方法已用数据证明到顶
（13 个内容无关特征最好可分性仅 0.40σ，要 3σ 才可靠），需要专门的水印检测模型。
LaMA 只解决「有了 mask 怎么补」，不解决「mask 从哪来」。
图片若遇上**大面积不透明水印**，经典重绘的效果有限，
届时可考虑接 LaMA / IOPaint 这类模型做后端（当前刻意不引入，避免重依赖）。

## 测试

```bash
cd /home/zcl/.pi/agent/skills/watermark-remover
.venv/bin/python -m pytest tests/ -q
```

测试用真实案例（`~/workspace/chat/郑清婉姓名贴.pdf`）断言：
辅助线必须判为线条、真水印不能判为线条、正文汉字必须是文字。
